import 'package:flutter/material.dart';

class HomeDesktopProfile extends StatelessWidget {
  const HomeDesktopProfile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
